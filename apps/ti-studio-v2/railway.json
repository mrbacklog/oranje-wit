{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/ti-studio-v2/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE"
  }
}

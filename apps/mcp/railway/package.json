{
  "name": "railway-mcp",
  "version": "2.0.0",
  "description": "MCP server voor Railway platform management via GraphQL API v2",
  "main": "server.js",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}

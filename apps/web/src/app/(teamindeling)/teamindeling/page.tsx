export const dynamic = "force-dynamic";

export default async function TeamIndelingMobileDashboard() {
  return (
    <div style={{ padding: "1rem" }}>
      <h1
        style={{
          fontSize: "1.5rem",
          fontWeight: 700,
          color: "var(--text-primary)",
          marginBottom: "0.5rem",
        }}
      >
        Team-Indeling
      </h1>
      <p style={{ color: "var(--text-secondary)" }}>Mobile versie — in ontwikkeling</p>
    </div>
  );
}
